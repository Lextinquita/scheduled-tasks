name: Test Setup

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Check secrets are configured
        env:
          ACCOUNT_SID: ${{ secrets.ACCOUNT_SID }}
          AUTH_TOKEN: ${{ secrets.AUTH_TOKEN }}
          MY_KEY: ${{ secrets.MY_KEY }}
          MY_LAT: ${{ secrets.MY_LAT }}
          MY_LNG: ${{ secrets.MY_LNG }}
          MY_FROM: ${{ secrets.MY_FROM }}
          MY_TO: ${{ secrets.MY_TO }}
        run: |
          if [ -z "$ACCOUNT_SID" ]; then
            echo "ACCOUNT_SID secret is not set!"
            echo "Go to Settings > Secrets > Actions and add it."
            exit 1
          else
            echo "ACCOUNT_SID is configured"
          fi

          if [ -z "$AUTH_TOKEN" ]; then
            echo "AUTH_TOKEN secret is not set!"
            echo "Go to Settings > Secrets > Actions and add it."
            exit 1
          else
            echo "AUTH_TOKEN is configured"
          fi

          if [ -z "$MY_KEY" ]; then
            echo "MY_KEY secret is not set!"
            echo "Go to Settings > Secrets > Actions and add it."
            exit 1
          else
            echo "MY_KEY is configured"
          fi

           if [ -z "$MY_LAT" ]; then
            echo "MY_LAT secret is not set!"
            echo "Go to Settings > Secrets > Actions and add it."
            exit 1
          else
            echo "MY_LAT is configured"
          fi
          
          if [ -z "$MY_LNG" ]; then
            echo "MY_LNG secret is not set!"
            echo "Go to Settings > Secrets > Actions and add it."
            exit 1
          else
            echo "MY_LNG is configured"
          fi
          if [ -z "$MY_TO" ]; then
            echo "MY_TO secret is not set!"
            echo "Go to Settings > Secrets > Actions and add it."
            exit 1
          else
            echo "MY_TO is configured"
          fi
          if [ -z "$MY_FROM" ]; then
            echo "MY_FROM secret is not set!"
            echo "Go to Settings > Secrets > Actions and add it."
            exit 1
          else
            echo "MY_FROM is configured"
          fi
          echo ""
          echo "All secrets are configured!"

      - name: Check required files exist
        run: |
          echo "Checking for required files..."

          if [ -f "main.py" ]; then
            echo "main.py found"
          else
            echo "main.py not found!"
            exit 1
          fi

          if [ -f "requirements.txt" ]; then
            echo "requirements.txt found"
          else
            echo "requirements.txt not found!"
            exit 1
          fi

          echo ""
          echo "All required files found!"

      - name: Test Python syntax
        run: python -m py_compile main.py && echo "Python syntax OK"

      - name: Success
        run: |
          echo ""
          echo "========================================"
          echo "  All checks passed!"
          echo "  Your setup is ready to go."
          echo "========================================"
